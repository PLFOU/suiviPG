
---

### 📥 Base de données

Tu peux télécharger ta base SQLite ici :  
👉 [Télécharger `poids_tracker.db`](sandbox:/mnt/data/poids_tracker.db)

Place-la dans le même dossier que `app.py` une fois téléchargée.

---

Si tu veux, je peux aussi t'aider à **initialiser ton dépôt GitHub**, y créer un `.git` et t’expliquer comment le publier. Tu veux que je te guide pour ça ?
